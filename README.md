# GO API REST

## Instalando pacotes
`go get <caminho_do_pacote>`
- Exemplo: `go get github.com/gin-gonic/gin`

## Inicializando o projeto
`go mod init <nome_do_projeto>`

## Rodando o projeto
`go run main.go` _após criar o main.go_